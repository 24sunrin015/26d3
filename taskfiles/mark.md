2026년도 전국기능경기대회 채점기준

|  |  |  |
| --- | --- | --- |
| 1. 채점상의 유의사항 | 직 종 명 | 클라우드컴퓨팅 |
| ※ 다음 사항을 유의하여 채점하시오.    1) AWS의 지역은 ap-northeast-2을 사용합니다.  2) 웹페이지 접근은 크롬이나 파이어폭스를 이용합니다.  3) 웹페이지에서 언어에 따라 문구가 다르게 보일 수 있습니다.  4) shell에서의 명령어의 출력은 버전에 따라 조금 다를 수 있습니다.  5) 문제지와 채점지에 있는 <> 는 변수입니다. 해당 부분을 변경해 입력합니다.  6) 채점은 문항 순서대로 진행해야 합니다.  7) 삭제된 채점자료는 되돌릴 수 없음으로 유의하여 진행하며, 이의신청까지 완료 이후 선수가 생성한 클라우드 리소스를 삭제합니다.  8) 부분 점수가 있는 문항은 채점 항목에 부분 점수가 적혀져 있습니다.  9) 부분 점수가 따로 없는 문항은 모두 맞아야 점수로 인정됩니다.  10) 리소스의 정보를 읽어오는 채점항목은 기본적으로 스크립트 결과를 통해 채점을 진행하며, 만약 선수가 이의가 있다면 명령어를 직접 입력하여 확인해볼 수 있습니다.  11) [ ] 기호는 채점에 영향을 주지 않습니다.  12) 채점 내용의 $ 기호는 명령어에 포함되는 것이 아니라 쉘을 의미합니다. | | |

2. 채점기준표

|  |  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1) 주요항목별 배점 | | | 직 종 명 | | | 클라우드컴퓨팅 | | |
| 과제  번호 | 일련  번호 | 주요항목 | 배점 | 채점방법 | | 채점시기 | | 비고 |
| 독립 | 합의 | 경기  진행중 | 경기  종료후 |
| 제3과제 | 1 | 비정상 요청 처리 | 4 |  | ○ |  | ○ |  |
| 2 | 고가용성 및 안정성 | 12 |  | ○ |  | ○ |  |
| 3 | 성능 효율성 | 12 |  | ○ |  | ○ |  |
| 4 | 비용 최적화 | 12 |  | ○ |  | ○ |  |
| 합 계 | | | 40 |  |  |  |  |  |

2) 채점방법 및 기준

|  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| 과제  번호 | 일련  번호 | 주요항목 | 일련  번호 | 세부항목(채점방법) | 배점 |
| 3과제 | 1 | 비정상 요청 처리 | 1 | Image 처리율 >= 90% | 0.5 |
| 2 | Image 처리율 >= 85% | 0.5 |
| 3 | Image 처리율 >= 80% | 0.5 |
| 4 | Image 처리율 >= 50% | 0.5 |
| 5 | 비정상 요청 처리율 >= 90% | 0.5 |
| 6 | 비정상 요청 처리율 >= 85% | 0.5 |
| 7 | 비정상 요청 처리율 >= 80% | 0.5 |
| 8 | 비정상 요청 처리율 >= 50% | 0.5 |
| 2 | 고가용성 및 안정성 | 1 | user API 로드 처리 >= 90.0% | 0.5 |
| 2 | user API 로드 처리 >= 87.5% | 0.5 |
| 3 | user API 로드 처리 >= 85.0% | 0.5 |
| 4 | user API 로드 처리 >= 82.5% | 0.5 |
| 5 | user API 로드 처리 >= 80.0% | 0.5 |
| 6 | user API 로드 처리 >= 70.0% | 0.5 |
| 7 | user API 로드 처리 >= 50.0% | 0.5 |
| 8 | user API 로드 처리 >= 30.0% | 0.5 |
| 9 | product API 로드 처리 >= 90.0% | 0.5 |
| 10 | product API 로드 처리 >= 87.5% | 0.5 |
| 11 | product API 로드 처리 >= 85.0% | 0.5 |
| 12 | product API 로드 처리 >= 82.5% | 0.5 |
| 13 | product API 로드 처리 >= 80.0% | 0.5 |
| 14 | product API 로드 처리 >= 70.0% | 0.5 |
| 15 | product API 로드 처리 >= 50.0% | 0.5 |
| 16 | product API 로드 처리 >= 30.0% | 0.5 |
| 17 | stress API 로드 처리 >= 90.0% | 0.5 |
| 18 | stress API 로드 처리 >= 87.5% | 0.5 |
| 19 | stress API 로드 처리 >= 85.0% | 0.5 |
| 20 | stress API 로드 처리 >= 82.5% | 0.5 |
| 21 | stress API 로드 처리 >= 80.0% | 0.5 |
| 22 | stress API 로드 처리 >= 70.0% | 0.5 |
| 23 | stress API 로드 처리 >= 50.0% | 0.5 |
| 24 | stress API 로드 처리 >= 30.0% | 0.5 |

|  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| 과제  번호 | 일련  번호 | 주요항목 | 일련  번호 | 세부항목(채점방법) | 배점 |
| 3과제 | 3 | 성능 효율성 | 1 | (user API 로드 처리 <= 0.2 sec) >= 90.0% | 0.5 |
| 2 | (user API 로드 처리 <= 0.2 sec) >= 87.5% | 0.5 |
| 3 | (user API 로드 처리 <= 0.2 sec) >= 85.0% | 0.5 |
| 4 | (user API 로드 처리 <= 0.2 sec) >= 82.5% | 0.5 |
| 5 | (user API 로드 처리 <= 0.2 sec) >= 80.0% | 0.5 |
| 6 | (user API 로드 처리 <= 0.2 sec) >= 70.0% | 0.5 |
| 7 | (user API 로드 처리 <= 0.2 sec) >= 50.0% | 0.5 |
| 8 | (user API 로드 처리 <= 0.2 sec) >= 30.0% | 0.5 |
| 9 | (product API 로드 처리 <= 0.2 sec) >= 90.0% | 0.5 |
| 10 | (product API 로드 처리 <= 0.2 sec) >= 87.5% | 0.5 |
| 11 | (product API 로드 처리 <= 0.2 sec) >= 85.0% | 0.5 |
| 12 | (product API 로드 처리 <= 0.2 sec) >= 82.5% | 0.5 |
| 13 | (product API 로드 처리 <= 0.2 sec) >= 80.0% | 0.5 |
| 14 | (product API 로드 처리 <= 0.2 sec) >= 70.0% | 0.5 |
| 15 | (product API 로드 처리 <= 0.2 sec) >= 50.0% | 0.5 |
| 16 | (product API 로드 처리 <= 0.2 sec) >= 30.0% | 0.5 |
| 17 | (stress API 로드 처리 <= 1.0 sec) >= 90.0% | 0.5 |
| 18 | (stress API 로드 처리 <= 1.0 sec) >= 87.5% | 0.5 |
| 19 | (stress API 로드 처리 <= 1.0 sec) >= 85.0% | 0.5 |
| 20 | (stress API 로드 처리 <= 1.0 sec) >= 82.5% | 0.5 |
| 21 | (stress API 로드 처리 <= 1.0 sec) >= 80.0% | 0.5 |
| 22 | (stress API 로드 처리 <= 1.0 sec) >= 70.0% | 0.5 |
| 23 | (stress API 로드 처리 <= 1.0 sec) >= 50.0% | 0.5 |
| 24 | (stress API 로드 처리 <= 1.0 sec) >= 30.0% | 0.5 |
| 4 | 비용 최적화 | 1 | 0.5 <= 인스턴스 비용 ratio <= 1.00 | 1.0 |
| 2 | 0.5 <= 인스턴스 비용 ratio <= 1.25 | 1.0 |
| 3 | 0.5 <= 인스턴스 비용 ratio <= 1.50 | 1.0 |
| 4 | 0.5 <= 인스턴스 비용 ratio <= 1.75 | 1.0 |
| 5 | 0.5 <= 인스턴스 비용 ratio <= 2.00 | 1.0 |
| 6 | 0.5 <= 인스턴스 비용 ratio <= 2.25 | 1.0 |
| 7 | 0.5 <= 인스턴스 비용 ratio <= 2.50 | 1.0 |
| 8 | 0.5 <= 인스턴스 비용 ratio <= 2.75 | 1.0 |

|  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| 과제  번호 | 일련  번호 | 주요항목 | 일련  번호 | 세부항목(채점방법) | 배점 |
| 3과제 | 4 | 비용 최적화 | 9 | 0.5 <= 인스턴스 비용 ratio <= 3.00 | 1.0 |
| 10 | 0.5 <= 인스턴스 비용 ratio <= 3.25 | 1.0 |
| 11 | 0.5 <= 인스턴스 비용 ratio <= 3.50 | 1.0 |
| 12 | 0.5 <= 인스턴스 비용 ratio <= 3.75 | 1.0 |
| 총점 | | | | 40 |

3) 채점내용

|  |  |
| --- | --- |
| 순번 | 사전준비 |
| 0 | 1) 선수가 제출한 엔드포인트가 선수의 시스템이 맞는지 확인하고 틀리다면 모든 항목 0점 처리 합니다.  2) DB 타입 및 대수를 확인하고 다른 형태 및 문제 의도와 다르게 사용 시 “3. 성능 효율성”, “4. 비용 최적화” 항목을 모두 0점 처리합니다.  3) 기타 EC2 및 리소스 상태를 점검하고 다른 형태를 사용 시 채점항목에 0점을 부여할 수 있습니다.  4) Lambda 사용과 관련해 부적절한 형태로 사용 시 전체 0점 처리합니다. |

|  |  |
| --- | --- |
| 순번 | 채점 항목 |
| 1-1 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 image download 값이 90.0% 이상인지 확인합니다. |
| 1-2 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 image download 값이 85.0% 이상인지 확인합니다. |
| 1-3 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 image download 값이 80.0% 이상인지 확인합니다. |
| 1-4 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 image download 값이 50.0% 이상인지 확인합니다. |
| 1-5 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 Exception Handling 값이 90.0% 이상인지 확인합니다. |
| 1-6 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 Exception Handling 값이 85.0% 이상인지 확인합니다. |
| 1-7 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 Exception Handling 값이 80.0% 이상인지 확인합니다. |
| 1-8 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 Exception Handling 값이 50.0% 이상인지 확인합니다. |
| 2-1 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) availability 값이 90.0% 이상인지 확인합니다. |
| 2-2 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) availability 값이 87.5% 이상인지 확인합니다. |
| 2-3 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) availability 값이 85.0% 이상인지 확인합니다. |

|  |  |
| --- | --- |
| 순번 | 채점 항목 |
| 2-4 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) availability 값이 82.5% 이상인지 확인합니다. |
| 2-5 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) availability 값이 80.0% 이상인지 확인합니다. |
| 2-6 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) availability 값이 70.0% 이상인지 확인합니다. |
| 2-7 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) availability 값이 50.0% 이상인지 확인합니다. |
| 2-8 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) availability 값이 30.0% 이상인지 확인합니다. |
| 2-9 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) availability 값이 90.0% 이상인지 확인합니다. |
| 2-10 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) availability 값이 87.5% 이상인지 확인합니다. |
| 2-11 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) availability 값이 85.0% 이상인지 확인합니다. |
| 2-12 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) availability 값이 82.5% 이상인지 확인합니다. |
| 2-13 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) availability 값이 80.0% 이상인지 확인합니다. |
| 2-14 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) availability 값이 70.0% 이상인지 확인합니다. |

|  |  |
| --- | --- |
| 순번 | 채점 항목 |
| 2-15 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) availability 값이 50.0% 이상인지 확인합니다. |
| 2-16 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) availability 값이 30.0% 이상인지 확인합니다. |
| 2-17 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) availability 값이 90.0% 이상인지 확인합니다. |
| 2-18 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) availability 값이 87.5% 이상인지 확인합니다. |
| 2-19 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) availability 값이 85.0% 이상인지 확인합니다. |
| 2-20 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) availability 값이 82.5% 이상인지 확인합니다. |
| 2-21 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) availability 값이 80.0% 이상인지 확인합니다. |
| 2-22 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) availability 값이 70.0% 이상인지 확인합니다. |
| 2-23 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) availability 값이 50.0% 이상인지 확인합니다. |
| 2-24 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) availability 값이 30.0% 이상인지 확인합니다. |
| 3-1 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 90.0% 이상인지 확인합니다. |

|  |  |
| --- | --- |
| 순번 | 채점 항목 |
| 3-2 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 87.5% 이상인지 확인합니다. |
| 3-3 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 85.0% 이상인지 확인합니다. |
| 3-4 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 82.5% 이상인지 확인합니다. |
| 3-5 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 80.0% 이상인지 확인합니다. |
| 3-6 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 70.0% 이상인지 확인합니다. |
| 3-7 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 50.0% 이상인지 확인합니다. |
| 3-8 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30.0% 이상인지 확인합니다. |
| 3-9 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 90.0% 이상인지 확인합니다. |
| 3-10 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 87.5% 이상인지 확인합니다. |
| 3-11 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 85.0% 이상인지 확인합니다. |
| 3-12 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 82.5% 이상인지 확인합니다. |

|  |  |
| --- | --- |
| 순번 | 채점 항목 |
| 3-13 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 80.0% 이상인지 확인합니다. |
| 3-14 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 70.0% 이상인지 확인합니다. |
| 3-15 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 50.0% 이상인지 확인합니다. |
| 3-16 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30.0% 이상인지 확인합니다. |
| 3-17 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 90.0% 이상인지 확인합니다. |
| 3-18 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 87.5% 이상인지 확인합니다. |
| 3-19 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 85.0% 이상인지 확인합니다. |
| 3-20 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 82.5% 이상인지 확인합니다. |
| 3-21 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 80.0% 이상인지 확인합니다. |
| 3-22 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 70.0% 이상인지 확인합니다. |
| 3-23 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 50.0% 이상인지 확인합니다. |

|  |  |
| --- | --- |
| 순번 | 채점 항목 |
| 3-24 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30.0% 이상인지 확인합니다. |
| 4-1 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 1.00 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-2 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 1.25 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-3 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 1.50 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-4 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 1.75 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-5 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 2.00 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-6 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 2.25 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |

|  |  |
| --- | --- |
| 순번 | 채점 항목 |
| 4-7 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 2.50 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-8 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 2.75 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-9 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 3.00 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-10 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 3.25 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-11 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 3.50 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
| 4-12 | 1) SSH를 통해 채점용 인스턴스에 접근합니다. (로드를 주입한 인스턴스)  2) results\_<비번호>.log 파일을 확인하여 cost ratio 값이 0.50 이상, 3.75 이하인지 확인합니다.  3) results\_<비번호>.log 파일을 확인하여 (user) performance 값이 30% 이상인지 확인합니다.  4) results\_<비번호>.log 파일을 확인하여 (product) performance 값이 30% 이상인지 확인합니다.  5) results\_<비번호>.log 파일을 확인하여 (stress) performance 값이 30% 이상인지 확인합니다. |
