# Debug Journal — user rollout timeout

Started: 2026-08-25
Goal: Find why the user Deployment did not become available after `make up`.

## Environment snapshot

- Runtime: EKS 1.35, Kubernetes client 1.35, AWS profile `sub2`.
- Entry: `AWS_PROFILE=sub2 STUDENT_ID=99001 make up`.
- Cluster: `apdev-eks` in `ap-northeast-2`.
- References read: debugging setup, investigation, fix, QA, cleanup.

## Hypotheses

1. [OPEN] user Pod is unschedulable on the apps node — evidence: Deployment events and Pod scheduling condition — fix: capacity or selector.
2. [OPEN] image pull or container startup fails — evidence: container state and Pod events — fix: image or config.
3. [OPEN] readiness probe fails because user cannot connect to RDS — evidence: Pod logs and readiness events — fix: runtime configuration.

## Artifacts to revert

- [ ] `.debug-journal.md` — remove before completing the deployment experiment.
- [ ] EKS Deployment rollback for `user`, `product`, `stress` — experiment-only recovery from the initial rollout; removed with the full Terraform destroy.

## Findings

### 2026-08-25 — user rollout
- Source: `kubectl describe pod user-655976f44-tmp7j`
- Value: `0/3 nodes are available: 1 Insufficient cpu, 2 node(s) had untolerated taint(s).`
- Interpretation: the new 512m user Pod cannot join the apps node while the old user Pod, product, and system Pods consume 1474m of its 1930m allocatable CPU.
- Refutes/Confirms: H1 confirms; H2 and H3 refuted by the existing Ready user Pod and successful health checks.
